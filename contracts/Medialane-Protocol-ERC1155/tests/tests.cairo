// Integration tests for the redesigned Medialane1155 venue are rebuilt test-first
// with in-Cairo snforge keypair signing (no StarknetJS, no hardcoded sigs).
// Schema-level unit tests live in src/core/types.cairo.
#[cfg(test)]
mod test {}
